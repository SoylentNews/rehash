__section__
default
__description__
* tab_selected and style are passed to createMenu

__title__

__page__
journal
__lang__
en_US
__name__
journalhead
__template__
[% USE Slash %]
[% IF useredit;
	Slash.createMenu("users",   {
		tab_selected => tab_selected,
		style => style,
		justify => "right"
	});
	PROCESS user_titlebar width => "100%" tab_selected => "journal";
	Slash.createMenu("journal", {
		style => 'oldstyle'
	});
ELSE;
	# what the hell is this for?
	Slash.createMenu("users",   {
		tab_selected => tab_selected, style => style
	});
	Slash.createMenu("journal", {
		tab_selected => tab_selected, style => style
	});
	PROCESS titlebar width => "100%";
END %]

__seclev__
500
